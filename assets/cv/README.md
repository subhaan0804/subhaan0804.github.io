Place your CV here as `subhaan-cv-2026.pdf`.

Once added, update `data.json`:
```json
"cvUrl": "assets/cv/subhaan-cv-2026.pdf"
```

The "Download CV" button in the hero will activate automatically.
Keep this file updated — version it by year (e.g. subhaan-cv-2027.pdf) rather than overwriting.
